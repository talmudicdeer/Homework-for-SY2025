Alexis Baker


Data Structures


Game 3


11/13/2025

**What are the four main methods that characterize the deque API?**
Add, Remove, Get, and Size

**Translate the attached image into Java code. Assume that you start the list with Head  A,
and you insert tmp to create the list on top.**
See `Game3Q2.java`.

**What is a static variable in Java?**
A static variable is one that belongs to the class itself rather than any specific object within it, and belongs to all instances within it as a result.

**Give a recursive definition of a binary tree. Include the base case.**
A binary tree is either empty, meaning it has no nodes, or it consists of a root with two children, which are, themselves, binary trees.

**What is the big-O time complexity of searching a balanced binary tree? Include a tight
bound.**
O(log n)
