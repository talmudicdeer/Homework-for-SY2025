Alexis Baker


Data Structures


Game 1


11/25/2025


**Name a FIFO data structure and name 3 of its most important methods.**
The most ubiquitous FIFO data structure is the **queue** and 3 of its most important methods (well, its *only* methods) are `add()`, `remove()`, and `size()`.

**What is a static variable in Java?**
I've answered this question so many damn times by now, I'm actually laughing to myself a bit. A static variable belongs to the class it's declared in and all instances within that class.

**What is the average case time complexity of searching for a value in an array of size *N*?**
O(n)

**What is the average case time complexity of retrieving a value by its index in an array of size *N*?**
O(1)